{
  "name": "constx-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "ConstX Monorepo — constx-core (utility library) + constx (fullstack framework) 🇮🇩",
  "scripts": {
    "build:core": "cd packages/core && npm run build",
    "build:constx": "cd packages/constx && npm run build",
    "build:all": "npm run build:core && npm run build:constx",
    "publish:core": "cd packages/core && npm publish",
    "publish:constx": "cd packages/constx && npm publish"
  }
}
